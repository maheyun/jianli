# ============================================
# 每天早上：把 GitHub 上的代码拉到本地
# ============================================
git pull origin main

# ============================================
# 每天晚上：把本地代码推到 GitHub
# ============================================
git add .
git commit -m "日常更新"
git push origin main
