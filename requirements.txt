# ============================================================
# 跃动体育 数据分析项目 — 依赖库清单
# ============================================================
#
# ┌──────────────────────────────────────────────────┐
# │  推荐用 conda（一次装完，自动处理依赖冲突）:         │
# │                                                    │
# │    conda install -c conda-forge \                  │
# │      numpy pandas matplotlib seaborn \             │
# │      scipy scikit-learn pymysql \                  │
# │      cryptography python-docx jupyter              │
# │                                                    │
# │  或者用 pip（如果你偏好 pip）:                       │
# │                                                    │
# │    pip install -r requirements.txt                 │
# └──────────────────────────────────────────────────┘
#
# 区别:
#   conda → 安装预编译的二进制包，更快，自动解决 C 库依赖
#   pip   → 从 PyPI 下载，某些包需要本地编译（如 scipy）
#
# 建议: 既然你已经建了 conda 虚拟环境，就用 conda install，
#       比 pip 省事。如果某个包 conda 找不到，再用 pip 补。

# ── 核心数据处理 ──────────────────────────
numpy>=1.24,<2.0
pandas>=2.0

# ── 可视化 ────────────────────────────────
matplotlib>=3.7
seaborn>=0.12

# ── 统计分析 + 机器学习 ────────────────────
scipy>=1.10
scikit-learn>=1.3

# ── MySQL 数据库连接 ───────────────────────
pymysql>=1.1
cryptography>=41.0

# ── Word 文档读取（简历提取工具用到）────────
python-docx>=0.8

# ── Jupyter Notebook（运行 .ipynb 文件）────
jupyter>=1.0
